spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/a08_energy_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: xiaoe

  # 🤖 核心修复：ai 必须紧跟在 spring 下面（缩进2个空格），和 datasource 平级！
  ai:
    # 👇 1. 新增这块：强制干掉本地的英文瞎子模型！
    transformers:
      embedding:
        enabled: false
    openai:
      # 你们的通义千问 API Key
      api-key: sk-244e27e02e404ef3aa4aa5be5765f169
      # 兼容 OpenAI 格式的请求地址
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          # 指定要使用的模型名字
          model: qwen3-max
      embedding:
        options:
          model: text-embedding-v2

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true # 开启驼峰命名自动映射 (air_temperature -> airTemperature)